# ── Web framework ──────────────────────────────────────────────
fastapi==0.136.3
uvicorn==0.48.0
python-multipart==0.0.30        # required by OAuth2 form login (/auth/login/form)

# ── Database ───────────────────────────────────────────────────
sqlalchemy==2.0.50
asyncpg==0.31.0                 # async driver (app runtime)
psycopg2-binary==2.9.12         # sync driver (for Alembic migrations — milestone 1b)
alembic==1.18.4                 # database migrations

# ── Validation & settings ──────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0          # required by Pydantic EmailStr
python-dotenv==1.2.2

# ── Security / auth ────────────────────────────────────────────
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                   # pinned explicitly (passlib 1.7.4 + bcrypt 5.x warns)

# ── AI provider (Groq via OpenAI-compatible client) ────────────
openai==2.41.0

# ── Rate limiting ──────────────────────────────────────────────
slowapi==0.1.9                  # IP-based throttling on auth endpoints

# ── Testing (dev only) ─────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1                   # async test client (also a transitive dep of openai)
