// Centralized route path strings — import these instead of hard-coding paths.
export const ROUTES = {
  DASHBOARD: '/',
  TICKETS: '/tickets',
  TICKET_NEW: '/tickets/new',
  LOGIN: '/login',
  REGISTER: '/register',
} as const
